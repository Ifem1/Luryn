import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { ConsensusPanel, SafetyBanner } from "@/components/Domain";
const steps=[['01','Charter a specimen','Declare synthetic assets, expected callers, monitored selectors, and prohibited responses.'],['02','Observe a trace','A keeper submits only a real testnet transaction hash and optional bounded session key.'],['03','Learn defensively','GenLayer validators independently check public evidence and store a compact, auditable finding.']];
export default function Home(){return <AppShell><section className="hero"><div><div className="eyebrow">Defensive decoy intelligence · GenLayer</div><h1>Let hostile probes teach the defense.</h1><p className="lead">Luryn is an experimental field lab for teams operating safe, bounded smart-contract decoys on test networks. It classifies observed behavior—not people—and turns supported signals into mitigation-oriented lessons.</p><p><Link className="button" href="/labs/new">Create a testnet lab</Link></p></div><ConsensusPanel/></section><SafetyBanner/><section className="section"><div className="eyebrow">The observation cycle</div><div className="grid">{steps.map(([n,t,d])=><article className="tile" key={n}><div className="number">{n}</div><h3>{t}</h3><p>{d}</p></article>)}</div></section><section className="section"><h2>One question, independently judged.</h2><p className="lead">What does this observed interaction appear to be trying to do to this declared defensive decoy, and what defensive lesson can responsibly be extracted from it?</p></section></AppShell>}
