import { AppShell } from "@/components/AppShell"; import { SafetyBanner } from "@/components/Domain";
export default function Safety(){return <AppShell><section className="route"><div className="eyebrow">Safety boundary</div><h1 style={{fontSize:'clamp(38px,5vw,62px)'}}>A canary, never a trap.</h1><p>Luryn supports explicitly owned, synthetic-asset testnet decoys. It does not ship malicious token mechanics, collect credentials, generate exploit payloads, retaliate, or identify people. A classification is an evidence-limited hypothesis about a public interaction.</p><SafetyBanner/></section></AppShell>}
