import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { SafetyBanner } from "@/components/Domain";
export default async function Route({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;const path='/'+slug.join('/');return <AppShell><section className="route"><div className="eyebrow">Chain-backed workspace</div><h1 style={{fontSize:'clamp(36px,5vw,58px)'}}>{path === '/labs' ? 'Labs' : 'This surface awaits chain configuration.'}</h1><div className="empty"><span className="tag">CONFIGURATION REQUIRED</span><p>Luryn does not fabricate workspaces, decoys, interactions, verdicts, or transaction states. Set <span className="mono">NEXT_PUBLIC_LURYN_CONTRACT_ADDRESS</span> after a verified Studionet deployment, then this route reads authoritative contract data.</p></div><p><Link className="button alt" href="/about/safety">Read the safety boundary</Link></p><SafetyBanner/></section></AppShell>}
