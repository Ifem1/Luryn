import { AppShell } from "@/components/AppShell";
export default function Patterns(){return <AppShell><section className="route"><div className="eyebrow">Defensive finding index</div><h1 style={{fontSize:"clamp(38px,5vw,62px)"}}>Pattern cabinet</h1><p className="lead">A place to review on-chain findings after related observations have been grouped and mitigations recorded.</p><div className="empty"><span className="tag">ON-CHAIN DATA</span><p>Open Labs, then use the verified lifecycle writer to group an interaction into a finding. The cabinet intentionally does not show seeded findings.</p></div></section></AppShell>}
